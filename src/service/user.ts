// import { IUser } from '../model/user';

export class UserService {
  public get(_id: number, _name?: string) {
    return {
      id: 1,
      email: 'email',
      name: 'name',
      age: 20,
      status: 1,
    };
  }
}
